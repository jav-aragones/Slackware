#!/bin/bash 
#exec ck-launch-session dbus-launch --sh-syntax --exit-with-session sway
#
#
# Este comando se usa para iniciar sway. Permite la gestión de pendrivers.
#
#
exec dbus-launch --sh-syntax --exit-with-session sway
